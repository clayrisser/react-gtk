import render from './render';

const Button = 'Button';
const Entry = 'Entry';
const Spinner = 'Spinner';
const Window = 'Window';

export { render, Button, Entry, Spinner, Window };
export default { render, Button, Entry, Spinner, Window };
