{
  "name": "react-gtk",
  "description": "cross-platform react native desktop applications",
  "version": "0.0.1",
  "files": [
    "packages"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap && npm run link",
    "bootstrap:hoist": "lerna bootstrap --hoist && npm run link",
    "clean": "lerna clean -y",
    "generate": "cd generate && npm run start && cd ../packages/binding && npm run format",
    "lerna": "lerna",
    "link": "lerna link",
    "ls": "lerna ls",
    "nuke": "bash generate/nuke.sh",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.2.1",
    "rimraf": "^2.6.3"
  },
  "license": "MIT",
  "packageManager": "yarn@3.7.0",
  "dependencies": {
    "node-gtk": "^0.12.0"
  }
}
